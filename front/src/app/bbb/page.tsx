"use client";
import LoginModal from "../../../components/modal-components/login-modal";

export default function BBB() {
  return (
    <div>
      <LoginModal
        onClose={() => {}}
        onSwitchSignup={() => {}}
        onSubmit={() => {}}
      />
    </div>
  );
}
